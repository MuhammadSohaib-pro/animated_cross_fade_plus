name: animated_cross_fade_plus
description: "A Flutter package that provides an enhanced version of AnimatedCrossFade with support for multiple children and customizable animations."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
